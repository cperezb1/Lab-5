<h1>Mensajes</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Chat ID</th>
      <th>Usuario ID</th>
      <th>Mensaje</th>
      <th>Acción</th>
    </tr>
  </thead>
  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= message.chat_id %></td>
        <td><%= message.user_id %></td>
        <td><%= message.body %></td>
        <td><%= link_to "Ver", message_path(message), class: "btn btn-primary btn-sm" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
