<div class="container mt-5 d-flex justify-content-center">
  <div class="card p-4" style="min-width: 400px;">
    <h2 class="mb-4">Change your password</h2>

    <%= form_with model: resource, as: resource_name, url: password_path(resource_name), local: true do |form| %>
      <%= form.hidden_field :reset_password_token %>

      <div class="mb-3">
        <%= form.label :password %>
        <%= form.password_field :password, autocomplete: "new-password", class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :password_confirmation %>
        <%= form.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
      </div>

      <%= form.submit "Change my password", class: "btn btn-primary w-100" %>
    <% end %>
  </div>
</div>
